<%= open_popup(title: "#{@order.name} - #{@order.status_text}<br /><small>#{format_date(@order.created_at)} - #{@supplier.name}</small>".html_safe, partial: 'orders/details', locals: { order: @order }) %> 