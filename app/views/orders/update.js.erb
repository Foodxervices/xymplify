<% if @success %>
  $('#cart-details').html("<%= j render 'carts/details' %>")
  $('#popup').modal('hide');
<% else %>
  <%= open_popup(title: "#{@order.name} - #{@order.supplier&.name}", partial: 'orders/form', locals: { order: @order }) %> 
<% end %> 