dependencies:
  pre:
    - gem install bundler

deployment:
  staging:
    branch: master
    heroku:
      appname: foodhub-ror
    commands:
      - heroku run rake db:migrate 
      - heroku restart