database:
  override:
    - bundle exec rake db:create db:schema:load

dependencies:
  pre:
    - gem install bundler

deployment:
  production:
    branch: master
    heroku:
      appname: foodhub-ror