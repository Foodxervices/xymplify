database:
  override:
    - rake db:test:prepare

dependencies:
  pre:
    - gem install bundler

deployment:
  staging:
    branch: master
    commands:
      - git push git@heroku.com:foodhub-ror.git master
      - heroku run --app foodhub-ror
      - heroku run rake db:migrate 
      - heroku restart